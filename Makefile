dev: 				# start server is dev mod with hmr

start-dev: 			# start server in dev 

start-prod:			# start server in producution

build:				# build client assets

build-prod:			# build client assets for production

build-app:			# will build the app for all configured cordova platforms 

test:				# runs the tests